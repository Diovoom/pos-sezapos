Big scope — 8 areas across signup verification, an email settings page, receipt delivery (print / email / SMS), templates, receipt history, and security. I'll ship it in 3 phases and need one decision from you before I start Phase 3 (SMS).

---

### Good news up front

- Your Lovable email domain `notify.sezapos.com` is **verified**. Sending from the app will work as soon as I scaffold the templates and send route.
- The email queue, send log, suppression list, unsubscribe tokens, and retry/DLQ machinery are already in place.
- Supabase already sends the signup verification email; it just isn't styled yet. I will replace it with a branded template — no separate custom token table needed, which is the more secure choice (Supabase handles hashing, single-use, and expiry).

---

### Phase 1 — Verified merchant signup (email)

1. Scaffold the auth email templates (`signup`, `magiclink`, `recovery`, `email_change`) — branded SEZA POS look. The signup email becomes the "verify your account" link that lands the user in `/pos` where the onboarding wizard already runs.
2. On `/signup` "Check your inbox" screen: add a **Resend verification email** button (calls `supabase.auth.resend`) with a 60s cooldown.
3. **Developer helper** (only when `import.meta.env.DEV`): after signup, query `email_send_log` for the latest signup email to that address and show the message/status. Verification links are single-use tokens generated by Supabase Auth — Lovable doesn't expose the raw link. I'll surface delivery status + a "Resend" button so you can test end-to-end.
4. The trial + owner + store are already auto-created by the `handle_new_user` trigger on first sign-in (built in the previous turn). No changes needed.

### Phase 2 — Transactional email + Email Settings + Email Receipt + Receipt History

1. Scaffold transactional email (`/lovable/email/transactional/send` route, template registry, sample templates).
2. Build **receipt-email** template (logo, store info, items, discounts, taxes, total, payment, QR to hosted receipt, return policy).
3. Add **Email Settings** page under Settings:
   - Sender name, sender email (from `notify.sezapos.com`), reply-to.
   - Connection status card: shows "Connected — notify.sezapos.com verified" (green), with a **Send test email** button that POSTs a real test send and shows success/failure.
   - Persist settings on `stores` (new columns `email_sender_name`, `email_reply_to`).
   - No SMTP fields — Lovable Emails is managed, so exposing SMTP host/port would be misleading. If you want raw SMTP override, tell me and I'll add a Brevo/Resend connector path instead.
4. Wire **Email Receipt** button in the existing `ReceiptDialog`: prompt for/prefill customer email, validate, send.
5. Add public **`/r/$id`** hosted receipt page (read-only, no login) so email/SMS links resolve to a real page and QR codes work.
6. **Receipt History** page (new `/receipts` route): search by receipt #, customer name/email/phone, date, cashier. Row actions: reprint, resend email, resend SMS.
7. `sales` table: add `customer_email`, `customer_phone`, `customer_name` (optional). Backfill left null.

### Phase 3 — SMS receipts + polished print (needs your input)

1. **Print**: convert the current receipt render to a proper thermal-format print stylesheet (80mm width, monospace, logo, QR). Preview and printout will match.
2. **SMS**: Lovable does not ship a native SMS provider. Pick one and I'll wire it via the connector gateway:
   - **Twilio** (broad reach, pay-per-message)
   - **GatewayAPI** (Europe-friendly)
   - **Brevo SMS** (if you want one vendor for email + SMS)
   - **Skip SMS for now** — hide the Text Receipt button until you connect a provider.

---

### One decision to unblock everything

**Which SMS provider should I wire for Phase 3?** (Twilio / GatewayAPI / Brevo / Skip.) You'll be prompted to connect it — I don't need any keys pasted in chat.

Reply with the choice and I'll start Phase 1 immediately.